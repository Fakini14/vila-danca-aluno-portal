# Environment Variables Example
# Copy this file to .env.local for development
# These variables will be configured in Vercel dashboard for production

# Supabase Configuration (Required)
# Get these from your Supabase project settings
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_key_here

# Application URL (Optional)
# Used for redirect URLs and webhooks
VITE_APP_URL=http://localhost:8080

# Notes:
# - Never commit real API keys to version control
# - Use the new sb_publishable_ key format (not the old anon key)
# - Supabase Edge Function secrets should be configured in Supabase dashboard
# - ASAAS payment gateway secrets are managed in Supabase Edge Functions